services:
  - type: web
    name: free-relay
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: 22.16.0
      - key: PUBLIC_NODE_URL
        value: https://free-relay.onrender.com
